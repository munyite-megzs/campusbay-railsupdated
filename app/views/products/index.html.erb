<h1>All Products</h1>

<ul id="products">
  <% @products.each do |product| %>
    <li>
    <div class="card-panel">
      <%= link_to product.productTitle, product_path(product) %>
    </div>
    </li>
  <% end %>
</ul>